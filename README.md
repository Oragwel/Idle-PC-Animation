# IdlePC Auto-Shutdown

## Overview
This project is a web-based system that automatically shuts down idle PCs after 30 minutes of inactivity on a local area network (LAN).

## How to Run

1. Install dependencies:
   ```bash
   npm install
